version: '3.3'

services:
    batch_unkown_name:
      image: pythonsnippet
      networks:
        - infrastructure_test_network
      environment:
        - SHOULD_FAIL=${SHOULD_FAIL_2}

networks:
  infrastructure_test_network:
    external: true